local ComponentExtensions = {}

ComponentExtensions.IsClass = require(script.IsClass)
ComponentExtensions.RequiresComponent = require(script.RequiresComponent)
ComponentExtensions.RequiresInstance = require(script.RequiresInstance)
ComponentExtensions.RequiresAttribute = require(script.RequiresAttribute)
ComponentExtensions.GetInstance = require(script.GetInstance)
ComponentExtensions.GetComponents = require(script.GetComponents)

return ComponentExtensions